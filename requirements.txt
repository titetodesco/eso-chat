# UI / runtime
streamlit==1.50.0
requests==2.32.3

# Data
pandas==2.2.2
numpy==1.26.4          # << importante: evita conflito com torch/onnxruntime
pyarrow==15.0.2        # parquet
pypdf==4.3.1           # leitura PDF
python-docx==1.1.0     # leitura DOCX

# Embeddings (Sentence-Transformers + Torch CPU)
transformers==4.41.2
sentence-transformers==3.0.1
torch==2.2.2
tqdm==4.66.4           # (dep. transitiva, mas deixo explícito)

